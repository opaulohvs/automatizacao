{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && apt-get update && apt-get install -y chromium chromium-driver"
  },
  "deploy": {
    "startCommand": "sh start.sh"
  }
}

